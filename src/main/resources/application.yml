server:
  port: 8000

spring:
  mvc:
    static-path-pattern: /static/**
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:8001/ghproject-no01?useUnicode=true&serverTimezone=Asia/Seoul&allowMultiQueries=true
    username: root
    password: toor
  servlet:
    multipart:
      max-file-size: 128MB
      max-request-size: 128MB


mybatis:
  mapper-locations:
    - /mappers/*.xml

file:
  path: C:/project/ghproject-no01/upload/
